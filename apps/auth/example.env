# NODE_ENV= # The runtime mode (development | production); controls logging and error detail.
# PORT= # Port the auth server binds to (defaults to 3000 if unset).
# CLIENT_URLS= # Comma-separated origins allowed to access auth routes (e.g. https://app.localhost:5173).
# BETTER_AUTH_SECRET= # Secret token used by better-auth middleware for signing cookies or tokens.
# BETTER_AUTH_URL= # Base URL of the authentication provider (e.g. https://better-auth.localhost).
# DATABASE_URL= # Full connection string for the auth database (postgres://user:pass@host:port/db).
