# NITRO_PORT= # Port Nitro dev server listens on (default 3000).
# NITRO_API_BASE_URL= # Internal base URL Nitro uses when making API requests to the backend.
# VITE_API_BASE_URL= # Client-side base URL for the API (used by fetchers in the browser).
# VITE_APP_NAME= # Display name used by the web UI and metadata tags.