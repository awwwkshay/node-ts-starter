# NODE_ENV= # API runtime mode (development | production) affecting telemetry.
# PORT= # HTTP port serving API requests (defaults to 8000 if unset).
# CLIENT_URLS= # Comma-separated allowed origins for client apps.
# DATABASE_URL= # Postgres connection string used by the API (postgres://user:pass@host:port/db).
